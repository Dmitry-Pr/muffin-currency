apiVersion: v1
kind: Service
metadata:
  name: {{ include "muffin-currency-chart.fullname" . }}
  labels:
    {{- include "muffin-currency-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "muffin-currency-chart.selectorLabels" . | nindent 4 }}
